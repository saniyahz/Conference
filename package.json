{
  "name": "riri-story-image-gen",
  "version": "1.0.0",
  "description": "SDXL image generation pipeline with masked inpainting for Riri kids story app",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "replicate": "^1.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
